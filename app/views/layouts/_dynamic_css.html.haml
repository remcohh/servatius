:css
  .topnav {
    background-color: #{band.present? ? band.topnav_color : 'black'};
    opacity: #{band.present? ? band.topnav_opacity : 1};
    }

  .topnav_font_color{
    color: #{band.present? ? band.topnav_font_color: 'white'} !important;
  }

  a{
    color: #{band.present? ? band.link_color: 'blue'};
  }


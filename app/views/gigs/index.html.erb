<h3 class="title">Gigs</h3>




<table class="table is-fullwidth">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th>Signed Up? </th>
      <th scope="col" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @future_gigs.each do |gig| %>
      <nav class="level is-mobile">
        <div class="level-left w60">
          <%= link_to "#{gig.when.to_formatted_s(:short)} #{gig.title}", gig %>
        </div>
        <div class="'level-right">
          <% if gig.members.include?(current_member)%>
            <span>Aangemeld (wijzig)</span>
          <% elsif false #cancellations %>
          <% else %>
              <a class="button is-success">Ja</a>
              <a class="button is-danger">Nee</a>
          <% end %>
        </div>

      </nav>


      <!--<tr >-->
        <!--<td scope="row">-->
          <!--<%= link_to "#{gig.when.to_formatted_s(:short)} #{gig.title}", gig %>-->
        <!--</td>-->
        <!--<% if gig.players.include? current_member %>-->
        <!--<td class="is-success">Yes!</td>-->
        <!--<% else %>-->
        <!--<td>-->
          <!--<a class="button is-info">Ja</a>-->
          <!--<a class="button is-warning">Nee</a>-->
        <!--</td>-->
        <!--<% end %>-->
      <!--</tr>-->
    <% end %>
  </tbody>
</table>


<h3 class="title">Past Gigs</h3>

<table class="table is-fullwidth">
  <thead>
    <tr>
      <th scope="col">Title</th>
      <th>Signed Up? </th>
      <th scope="col" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @past_gigs.each do |gig| %>
      <tr >
        <th scope="row"><%= link_to "#{gig.when.to_formatted_s(:short)} #{gig.title}" %></th>
        <% if gig.players.include? current_member %>
        <td class="is-success">Yes!</td>
        <% else %>
        <td class="is-danger">No</td>
        <% end %>
        <td><% if can? :edit, :gigs %><%= link_to 'Edit', edit_gig_path(gig) %> <% end %></td>
        <td><% if can? :destroy, :gigs %><%= link_to 'Destroy', gig, method: :delete, data: { confirm: 'Are you sure?' } %><% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if can? :manage, @gigs %>
  <%= link_to 'New Gig', new_gig_path %>
<% end %>
